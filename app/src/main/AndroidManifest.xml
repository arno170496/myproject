<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.project">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.Register"
            android:exported="true" />
        <activity
            android:name=".activities.UserActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.Reminders"
            android:label="Reminder Details"></activity>

        <activity android:name=".activities.NotificationView"
            android:label="Details of Notification"
            android:parentActivityName=".activities.Reminders">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.Reminders"/>
        </activity>

        <activity android:name=".activities.BpActivity" />
        <activity android:name=".activities.TempActivity" />
        <activity android:name=".activities.PulseActivity" />
        <activity android:name=".activities.Myprofileactivity" />
        <activity android:name=".activities.HelpActivity" />
        <activity android:name=".activities.Emergency_Calls" />
        <activity android:name=".activities.Medical_centers" />
        <activity android:name=".activities.MainActivity2" />
        <activity android:name=".activities.MainActivity3"/>


    </application>

</manifest>